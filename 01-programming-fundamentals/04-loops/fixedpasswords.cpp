#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main() {
    ios::sync_with_stdio(false); cin.tie(NULL);

    while(true) {
        int n; cin >> n;

        if(n == 1999) {
            cout << "Correct" << endl;
            break;
        }

        cout << "Wrong" << endl;
    }
}